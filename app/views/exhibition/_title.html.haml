.title
  .sell__block__form__name
    .form-group__name
      %label
        商品名
        %span.require 必須
        = form_with(url: "#", local: true, method: :get, class: "form") do |f|
          = f.text_field :keyword, class: "input", placeholder: "40文字まで"

      %label
        商品の説明
        -# 改行がされていない
        %span.require 必須
        = form_with(url: "#", local: true, method: :get, class: "form") do |f|
          = f.text_area :content, class: 'Form__inputContent', placeholder: '商品の説明（必須 1,000文字以内）（色、素材、重さ、定価、注意点など）例）2010年頃に1万円で購入したジャケットです。ライトグレーで傷はありません。あわせやすいのでおすすめです。'